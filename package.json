{
  "name": "mc-dp-icons",
  "displayName": "Datapack Icons",
  "description": "A polished, Minecraft-inspired icon suite for the modern developer's workflow.",
  "version": "5.0.0",
  "publisher": "SuperAnt",
  "author": {
    "name": "FuncFusion"
  },
  "icon": "assets/2.0/logo.png",
  "galleryBanner": {
    "color": "#313338",
    "theme": "dark"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.5.1"
  },
  "keywords": [
    "minecraft",
    "datapack",
    "resourcepack",
    "addon",
    "bedrock",
    "icons",
    "theme",
    "mc",
    "dp",
    "command",
    "block",
    "mcfunction",
    "mcf"
  ],
  "categories": [
    "Themes",
    "Visualization"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/FuncFusion/mc-dp-icons"
  },
  "homepage": "https://github.com/FuncFusion/mc-dp-icons",
  "activationEvents": [
    "onStartup",
    "onWorkspaceFolderOpened",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "browser": "./dist/webExtension.js",
  "contributes": {
    "iconThemes": [
      {
        "id": "mc-dp-icons",
        "label": "Datapack Icons Theme",
        "path": "./file_icon_themes/active.json",
        "_watch": true
      }
    ],
    "configuration": [
      {
        "title": "Datapack Icons",
        "order": 0
      },
      {
        "title": "Workspace Detection",
        "order": 1,
        "properties": {
          "mc-dp-icons.workspaceDetection": {
            "order": 0,
            "type": "boolean",
            "title": "Enable Workspace Detection",
            "default": false,
            "markdownDescription": "If enabled, the extension scans your workspace for Minecraft project files (`pack.mcmeta`, Bedrock `manifest.json`, Beet or JMC config files) and automatically applies the `mc-dp-icons` theme if detected. If not, it switches back to your `#mc-dp-icons.fallbackIconTheme#`.\n\nThis scan runs on startup and when modifying files."
          },
          "mc-dp-icons.fallbackIconTheme": {
            "order": 1,
            "type": "string",
            "title": "Fallback Icon Theme",
            "default": "",
            "markdownDescription": "*Requires `#mc-dp-icons.workspaceDetection#` to be enabled.*\n\nEnter the exact ID of the icon theme you want to use for non-Minecraft workspaces (e.g., `vs-seti` or `material-icon-theme`).\n\nIf blank, the extension defaults to the icon theme that was active before `mc-dp-icons` was applied."
          }
        }
      },
      {
        "id": "loadTick",
        "title": "Function Icons",
        "order": 2,
        "properties": {
          "mc-dp-icons.dynamicFunctionIcons": {
            "order": 0,
            "type": "boolean",
            "title": "Dynamic Function Icons",
            "default": true,
            "markdownDescription": "If enabled, functions listed in your `tick.json` and `load.json` files automatically receive repeating and impulse command block icons.\n\nDisable this to manually assign these icons using the name lists below.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.loadFunctionNames": {
            "order": 1,
            "title": "Load function names",
            "type": "array",
            "items": {
              "type": "string"
            },
            "markdownDescription": "*Requires `#mc-dp-icons.dynamicFunctionIcons#` to be disabled.*\n\nEnter the file names (without `.mcfunction`) that should receive the **impulse command block** icon.\n\n**Wildcards are supported:** Use `*` to match multiple files. For example, `setup_*` will match `setup_game.mcfunction` and `setup_settings.mcfunction`.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.tickFunctionNames": {
            "order": 2,
            "title": "Tick function names",
            "type": "array",
            "items": {
              "type": "string"
            },
            "markdownDescription": "*Requires `#mc-dp-icons.dynamicFunctionIcons#` to be disabled.*\n\nEnter the file names (without `.mcfunction`) that should receive the **repeating command block** icon.\n\n**Wildcards are supported:** Use `*` to match multiple files. For example, `*_loop` will match `game_loop.mcfunction` and `particle_loop.mcfunction`.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          }
        }
      },
      {
        "id": "crownedFunctions",
        "title": "Crowned Function Icons",
        "order": 3,
        "properties": {
          "mc-dp-icons.crownedFunctions": {
            "title": "Crowned Functions",
            "type": "array",
            "items": {
              "type": "string"
            },
            "markdownDescription": "Enter the file names (without `.mcfunction`) that should receive the **crowned chain command block** icon.\n\n**Wildcards are supported:** Use `*` to match multiple files. For example, `*utils*` matches `math_utils.mcfunction` and `utils-general.mcfunction`.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.crownedTickFunctions": {
            "title": "Crowned Tick Functions",
            "type": "array",
            "items": {
              "type": "string"
            },
            "markdownDescription": "Enter the file names (without `.mcfunction`) that should receive the **crowned repeating command block** icon.\n\n**Wildcards are supported:** Use `*` to match multiple files. For example, `*_main` matches `combat_main.mcfunction` and `spawn_main.mcfunction`.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.crownedLoadFunctions": {
            "title": "Crowned Load Functions",
            "type": "array",
            "items": {
              "type": "string"
            },
            "markdownDescription": "Enter the file names (without `.mcfunction`) that should receive the **crowned impulse command block** icon.\n\n**Wildcards are supported:** Use `*` to match multiple files. For example, `init_*` matches `init_scoreboards.mcfunction` and `init_entities.mcfunction`.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          }
        }
      },
      {
        "title": "Dynamic Icons",
        "order": 4,
        "properties": {
          "mc-dp-icons.subfolderIcons": {
            "type": "boolean",
            "order": 0,
            "title": "Enable Subfolder Icons",
            "default": true,
            "markdownDescription": "*Requires a `pack.mcmeta` file at the root of the pack.*\n\nIf enabled, a workaround activates that allows JSON files nested inside category subfolders to receive their specific icon rather than a generic one (e.g., `data/namespace/item_modifier/subfolder/file.json` receives the diamond pickaxe icon). *Not recommended for extremely large workspaces.*\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.namespaceIcons": {
            "type": "boolean",
            "order": 1,
            "title": "Namespace Icons",
            "default": true,
            "markdownDescription": "*Requires a `pack.mcmeta` file at the root of the pack.*\n\nIf enabled, namespace folders (directories directly within `data` and `assets`) receive an ender chest icon.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.overlayIcons": {
            "type": "boolean",
            "order": 2,
            "title": "Overlay Icons",
            "default": true,
            "markdownDescription": "If enabled, overlay folders registered in your `pack.mcmeta` receive a glass chest icon.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.christmasIcons": {
            "type": "string",
            "order": 3,
            "title": "Use Christmas icons",
            "default": "Only on Christmas",
            "enum": [
              "Only on Christmas",
              "Always",
              "Never"
            ],
            "markdownDescription": "Determines when chest icons switch to their festive Christmas variants.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          },
          "mc-dp-icons.hideFolderArrows": {
            "type": "boolean",
            "order": 4,
            "title": "Hide folder arrows",
            "default": false,
            "markdownDescription": "If enabled, hides the expand/collapse arrows next to folders in the VS Code Explorer.\n\n*Warning: This feature is not currently supported on VS Code Web.*"
          }
        }
      }
    ],
    "commands": [
      {
        "title": "Datapack Icons: Open extension settings",
        "command": "mc-dp-icons.DpIconsOpenSettings"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "build": "node esbuild.mjs",
    "build-dev": "node esbuild.mjs --watch",
    "package": "NODE_ENV=production node esbuild.mjs",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^18.19.130",
    "@types/vscode": "^1.5.1",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@vscode/test-cli": "^0.0.4",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/test-web": "^0.0.63",
    "esbuild": "^0.24.0",
    "eslint": "^8.57.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {}
}
